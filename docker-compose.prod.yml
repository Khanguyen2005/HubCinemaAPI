# Production docker-compose file
version: '3.8'

services:
  hubcinema-api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      # Override with external database and Redis if needed
      - ConnectionStrings__DefaultConnection=Data Source=160.30.44.204,14334;Initial Catalog=cinema;User ID=sa;Password=StrongPass@123;Encrypt=True;Trust Server Certificate=True
      - Redis__ConnectionString=redis-18049.crce264.ap-east-1-1.ec2.cloud.redislabs.com:18049,password=PjEAlhDFKaccsFIryc8YfQqOz2tGLaDX,abortConnect=False
    # Remove depends_on if using external services
    depends_on: []
